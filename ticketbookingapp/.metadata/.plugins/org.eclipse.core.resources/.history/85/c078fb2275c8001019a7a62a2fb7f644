/* ---------- AUTH (Login / Register) ---------- */

.auth-container {
    max-width: 380px;
    margin: 60px auto;
    padding: 28px 24px 26px;
    background: var(--bg-card);
    border-radius: 16px;
    border: 1px solid var(--border-light);
    box-shadow: 0 10px 30px rgba(0,0,0,0.7);
}

.auth-title {
    font-size: 1.4rem;
    font-weight: 600;
    margin-bottom: 4px;
}

.auth-subtitle {
    font-size: 0.9rem;
    color: var(--text-muted);
    margin-bottom: 18px;
}

.form-footer {
    margin-top: 10px;
    font-size: 0.85rem;
    color: var(--text-muted);
}

.form-footer a {
    color: var(--accent-red);
    text-decoration: none;
}

.form-footer a:hover {
    text-decoration: underline;
}

/* Alerts */
.alert {
    padding: 10px 12px;
    border-radius: 8px;
    font-size: 0.85rem;
    margin-bottom: 12px;
    border: 1px solid transparent;
}

.alert-success {
    background: rgba(76, 175, 80, 0.12);
    border-color: #4CAF50;
    color: #C8E6C9;
}

.alert-error {
    background: rgba(229, 9, 20, 0.12);
    border-color: var(--accent-red);
    color: #FF8A80;
}

/* ---------- MOVIE LIST (Home) ---------- */

.movie-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 20px;
    margin-top: 20px;
}

.movie-card {
    background: var(--bg-card-light);
    border-radius: 16px;
    border: 1px solid var(--border-light);
    padding: 14px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    box-shadow: 0 8px 25px rgba(0,0,0,0.7);
    transition: transform 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease;
}

.movie-card:hover {
    transform: translateY(-3px);
    border-color: var(--accent-red);
    box-shadow: 0 12px 35px rgba(0,0,0,0.9);
}

.movie-card-body {
    display: flex;
    gap: 16px;
}

.movie-card-poster-wrapper {
    flex-shrink: 0;
}

.movie-card-poster,
.movie-card-poster-placeholder {
    width: 110px;
    height: 160px;
    border-radius: 10px;
    object-fit: cover;
    background: #000;
    border: 1px solid var(--border-light);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
    color: var(--text-muted);
}

.movie-card-info h3 {
    margin: 0 0 4px;
    font-size: 1.1rem;
}

.movie-meta {
    font-size: 0.8rem;
    color: var(--text-muted);
    margin-bottom: 6px;
}

.movie-desc {
    font-size: 0.85rem;
    color: var(--text-muted);
    max-height: 60px;
    overflow: hidden;
}

/* ---------- MOVIE DETAILS PAGE ---------- */

.details-header h2 {
    margin: 0 0 6px;
}

.details-meta {
    font-size: 0.9rem;
    color: var(--text-muted);
}

.show-date-form {
    margin-top: 16px;
    display: flex;
    gap: 10px;
    align-items: center;
}

.show-date-form label {
    font-size: 0.9rem;
}

.show-date-form input[type="date"] {
    max-width: 180px;
}

/* ---------- USER DASHBOARD / MY BOOKINGS TABLE ---------- */

.text-muted {
    color: var(--text-muted);
}

.mt-2 {
    margin-top: 8px;
}

.mt-3 {
    margin-top: 16px;
}

/* ---------- SEAT SELECTION PAGE ---------- */

.booking-header-card {
    display: flex;
    gap: 22px;
    align-items: flex-start;
}

.booking-header-left .poster-preview {
    width: 150px;
    height: 220px;
    border-radius: 12px;
}

.booking-header-right h3 {
    margin: 4px 0 2px;
}

.details-meta span {
    color: var(--text-muted);
}

/* Seat legend */
.seat-layout-wrapper {
    margin-top: 18px;
}

.seat-legend {
    display: flex;
    flex-wrap: wrap;
    gap: 12px 24px;
    margin-bottom: 14px;
    font-size: 0.85rem;
}

.seat-legend-row {
    display: flex;
    align-items: center;
    gap: 8px;
}

.seat-legend-pill {
    width: 20px;
    height: 14px;
    border-radius: 999px;
    display: inline-block;
}

/* Legend colors */
.seat-legend-pill.premium {
    background: #B71C1C;
}

.seat-legend-pill.gold {
    background: #F9A825;
}

.seat-legend-pill.standard {
    background: #424242;
}

.seat-legend-pill.booked {
    background: #333333;
}

/* Seat layout table */
.seat-layout {
    width: 100%;
    border-collapse: collapse;
    margin-top: 6px;
}

.seat-row-label {
    padding-right: 10px;
    font-size: 0.8rem;
    color: var(--text-muted);
    white-space: nowrap;
}

.seat-layout td {
    border: none;
    padding: 4px 4px;
}

/* Aisle gap */
.seat-aisle {
    display: inline-block;
    width: 18px;
}

/* Seat pill */
.seat-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 30px;
    padding: 3px 0;
    border-radius: 999px;
    font-size: 0.78rem;
    border: 1px solid transparent;
    cursor: pointer;
}

/* Pill color coding */
.seat-premium {
    background: rgba(183,28,28,0.85);
    border-color: #FF5252;
}

.seat-gold {
    background: rgba(249,168,37,0.9);
    color: #000;
    border-color: #FFEE58;
}

.seat-standard {
    background: #333333;
    border-color: #555555;
}

/* Booked (disabled) */
input.seat-checkbox:disabled + .seat-pill {
    background: #151515;
    border-color: #444444;
    color: #555555;
    cursor: not-allowed;
    text-decoration: line-through;
}

/* Selected seat */
input.seat-checkbox:checked + .seat-pill {
    box-shadow: 0 0 0 2px var(--accent-red);
    transform: translateY(-1px);
}

/* Screen representation */
.screen-container {
    margin-top: 20px;
    display: flex;
    justify-content: center;
}

.screen-inner {
    text-align: center;
}

.screen-bar {
    width: 280px;
    height: 6px;
    border-radius: 999px;
    background: linear-gradient(to right, #444444, #FFFFFF, #444444);
    margin: 0 auto 4px;
}

.screen-label {
    font-size: 0.75rem;
    letter-spacing: 0.16em;
    color: var(--text-muted);
}

/* Seat summary */
.seat-summary {
    margin-top: 16px;
    padding: 10px 12px;
    border-radius: 10px;
    background: rgba(229, 9, 20, 0.08);
    border: 1px dashed var(--accent-red);
    font-size: 0.9rem;
}

/* ---------- BOOKING SUCCESS – TICKET STYLE ---------- */

.booking-success-card {
    max-width: 900px;
    margin: 32px auto;
}

.booking-success-wrapper {
    display: flex;
    gap: 24px;
    align-items: stretch;
    flex-wrap: wrap;
}

.booking-success-left .poster-preview {
    width: 200px;
    height: 300px;
    border-radius: 14px;
}

/* Ticket container */
.ticket {
    background: var(--bg-card);
    border-radius: 20px;
    border: 1px solid var(--border-light);
    box-shadow: 0 10px 35px rgba(0,0,0,0.9);
    overflow: hidden;
}

.ticket-main {
    padding: 20px 24px 14px;
}

.ticket-header h1 {
    margin: 8px 0 4px;
    font-size: 1.4rem;
}

.badge-success {
    display: inline-block;
    padding: 4px 10px;
    font-size: 0.7rem;
    border-radius: 999px;
    background: rgba(0, 200, 83, 0.1);
    color: #00E676;
    letter-spacing: 0.08em;
}

.ticket-details {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    row-gap: 12px;
    column-gap: 16px;
    margin-top: 14px;
}

.ticket-details .label {
    display: block;
    font-size: 0.7rem;
    color: var(--text-muted);
    letter-spacing: 0.08em;
}

.ticket-details .value {
    font-size: 0.96rem;
    margin-top: 3px;
}

/* Tear line */
.tear-line {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #000000;
    padding: 8px 0;
}

.tear-line .dashed {
    width: 100%;
    border-top: 1px dashed #555555;
}

.tear-line .circle {
    width: 16px;
    height: 16px;
    background: var(--bg-main);
    border-radius: 50%;
    position: absolute;
}

.tear-line .circle.left {
    left: -8px;
}

.tear-line .circle.right {
    right: -8px;
}

/* Bottom QR strip */
.ticket-tear {
    display: flex;
    justify-content: space-between;
    padding: 14px 20px 16px;
    background: #050505;
    gap: 16px;
}

.qr-section {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.qr-img {
    width: 90px;
    height: 90px;
    background: #FFFFFF;
    padding: 8px;
    border-radius: 12px;
}

.qr-text {
    margin-top: 6px;
    font-size: 0.75rem;
    color: var(--text-muted);
    text-align: center;
    max-width: 160px;
}

.ticket-meta {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 6px;
}

.ticket-meta .label {
    font-size: 0.7rem;
    color: var(--text-muted);
    letter-spacing: 0.08em;
}

.ticket-meta .value.small {
    font-size: 0.9rem;
}

/* Booking success button row */
.booking-success-actions {
    margin-top: 18px;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

/* ---------- RESPONSIVE ---------- */

@media (max-width: 900px) {
    .booking-header-card {
        flex-direction: column;
        align-items: center;
    }

    .booking-success-wrapper {
        flex-direction: column;
        align-items: center;
    }

    .ticket-details {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }
}
